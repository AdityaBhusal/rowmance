services:
  webapi:
    build:
      context: .
      dockerfile: Romance.WebAPI/Dockerfile
    container_name: romance-api
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development